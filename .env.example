# Client-side data: mock fixtures vs live SQLite (hydrate in app when implemented).
# unset or `true` → mocks (default)
# `false` → start empty; use Electron + db.* to fill state
VITE_USE_MOCK_DATA=true
